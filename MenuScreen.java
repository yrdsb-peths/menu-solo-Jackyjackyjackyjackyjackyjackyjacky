import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

public class MenuScreen extends World {
    public MenuScreen() {
      super(600, 400, 1);
      Button button = new Button();
      addObject(button, 250, 250);
    }
}